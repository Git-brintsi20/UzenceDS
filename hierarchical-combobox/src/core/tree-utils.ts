/**
 * Tree manipulation utilities — pure functions, zero React dependency.
 *
 * Responsible for:
 * - Flattening a nested tree into a flat list (for virtualization)
 * - Computing ancestor labels (for search context)
 * - Deriving indeterminate checkbox states from child selections
 *
 * Implementation coming in Phase 2.
 */

export {};
