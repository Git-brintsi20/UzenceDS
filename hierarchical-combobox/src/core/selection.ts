/**
 * Selection logic — pure functions, zero React dependency.
 *
 * Handles multi-select behavior:
 * - Toggling a node propagates selection to all descendants
 * - Parent nodes derive indeterminate/checked state from children
 *
 * Implementation coming in Phase 2.
 */

export {};
