/**
 * VirtualizedList — the scrollable container that renders only visible rows.
 *
 * Uses the math from core/virtualization.ts to determine which
 * TreeRow components to mount based on scroll position.
 *
 * Implementation coming in Phase 3.
 */

export {};
