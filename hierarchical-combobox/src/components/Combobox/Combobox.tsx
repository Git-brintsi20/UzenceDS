/**
 * HierarchicalCombobox — the root component.
 *
 * Composes the input trigger, dropdown panel, virtualized tree list,
 * and selected-tags display.
 *
 * Implementation coming in Phase 3.
 */

export {};
