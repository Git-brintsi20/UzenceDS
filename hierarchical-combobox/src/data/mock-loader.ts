/**
 * Mock async data loader — simulates API responses with configurable delay.
 *
 * Returns tree node data as if fetched from a server.
 * Used during development and in Storybook stories.
 *
 * Implementation coming in Phase 2.
 */

export {};
